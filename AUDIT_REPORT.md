# تقرير تدقيق بوت تداول MEXC

## الخلاصة التنفيذية

كانت هناك مشكلات فعلية في المستودع تؤثر في سرعة التفاعل وسلامة حفظ حالة الصفقات. أهم مشكلة كانت أن التطبيق يستخدم `data/trades.db` بينما المستودع يحتوي على قاعدة بيانات باسم `trades.db` في الجذر. لذلك كانت بيانات الصفقات الموجودة في الملف المرفوع لا تُقرأ من التطبيق، وكان التطبيق ينشئ قاعدة SQLite أخرى فارغة عند التشغيل.

كما كان التطبيق يبدّل تلقائياً إلى SQLite عند فشل الاتصال بـ PostgreSQL. هذا السلوك قد يجعل الصفقات والإعدادات تبدو محفوظة محلياً بينما تضيع عند إعادة تشغيل خدمة Railway إذا لم يتم إعداد PostgreSQL أو Volume دائم. لذلك لا يمكن اعتبار SQLite حلاً دائماً في بيئة النشر.

تم تطبيق إصلاحات مباشرة في الكود. شملت الإصلاحات توحيد مسار SQLite، إضافة ترحيل تلقائي للأعمدة الناقصة، تفعيل مهلة انتظار وWAL لـ SQLite، منع إغلاق الصفقة في السجل عند فشل أمر البيع، ونقل استدعاءات MEXC الحاجبة خارج حلقة Telegram غير المتزامنة. كما تم إصلاح حد التحويلات المتناقض مع الاختبارات، وإصلاح تحديث وقف الخسارة بعد تحقيق الأهداف.

## النتائج الرئيسية

| الأولوية | النتيجة | الأثر | الحالة |
|---|---|---|---|
| حرجة | فشل البيع كان قد يُسجل كصفقة مغلقة | قاعدة البيانات قد تقول إن الصفقة أُغلقت بينما الأصل ما زال موجوداً في MEXC | تم الإصلاح |
| حرجة | مسار SQLite لا يطابق ملف `trades.db` الموجود | التطبيق كان يقرأ قاعدة مختلفة وفارغة | تم الإصلاح |
| حرجة | SQLite المحلي غير دائم على Railway دون Volume أو PostgreSQL | فقدان الصفقات والإعدادات بعد إعادة إنشاء الخدمة | يتطلب إعداد نشر |
| عالية | استدعاءات `get_price` و`market_sell` و`get_balance` متزامنة داخل handlers | تأخر الأزرار وتجمّد البوت أثناء انتظار الشبكة | تم الإصلاح في المسارات الرئيسية |
| عالية | فشل أمر البيع كان يتبعه `close_trade` في الخروج الطارئ | فقدان التوافق بين السجل والمنصة | تم الإصلاح |
| متوسطة | schema قديمة لا تُرقّى تلقائياً إلى جميع الأعمدة المطلوبة | فشل حفظ أو قراءة بعض الحقول بعد تحديث الكود | تم الإصلاح |
| متوسطة | `MIN_TRANSFERS=3` بينما الاختبارات والسلوك المقصود يتطلبان 10 | ظهور فرص ضعيفة واختبارات فاشلة | تم الإصلاح إلى 10 |
| متوسطة | تهيئة قاعدة البيانات تتكرر قبل عمليات كثيرة | تكلفة زمنية إضافية، خصوصاً مع PostgreSQL | ما زالت قابلة للتحسين لاحقاً |

## تدقيق الحفظ في قاعدة البيانات

### SQLite

آلية الحفظ الأساسية صحيحة من ناحية استخدام معاملات SQLite. عمليات الإدخال والتحديث تُنفذ داخل context manager، وتُحفظ عند الخروج الطبيعي من الكتلة. دورة الاختبار التالية نجحت: إنشاء الإعدادات، تعديلها، فتح صفقة، قراءتها، تسجيل هدف، إغلاق الصفقة، ثم قراءة السجل المغلق.

تم تعديل المسار الافتراضي إلى `trades.db` في جذر المشروع، مع دعم متغير البيئة `SQLITE_DB_PATH`. وتمت إضافة `timeout=30` و`busy_timeout=30000` و`journal_mode=WAL` لتقليل تعارضات الكتابة عند وجود أكثر من مهمة.

### PostgreSQL

التطبيق يدعم PostgreSQL عبر متغيرات مثل `DATABASE_URL`. الاتصال يستخدم `autocommit=True`، وهذا مناسب للعمليات القصيرة الحالية لكنه يجعل كل statement وحدة حفظ مستقلة. لا توجد معاملة متعددة الخطوات حول تنفيذ الأمر ثم تسجيل الصفقة، ولذلك يبقى خطر الانفصال بين المنصة وقاعدة البيانات قائماً إذا نجح أمر التداول وفشل التسجيل بعده.

تمت إضافة ترقية تلقائية لجدول `trades` عند وجود أعمدة ناقصة. الترقية لا تحذف السجل القديم، وتضيف أعمدة `side` و`contract` و`chain` وحقول الأهداف والإغلاق والملاحظات عند الحاجة.

### الخطر التشغيلي المتبقي

إذا وُجد `DATABASE_URL` لكنه فشل في الاتصال، فالكود الحالي يعود إلى SQLite. هذا يحافظ على استمرار البوت لكنه غير مناسب لحساب تداول حقيقي، لأن الحفظ قد يصبح محلياً ومؤقتاً دون انتباه المشغّل. في الإنتاج يجب إعداد PostgreSQL صالح، ثم التأكد من ظهور حالة `PostgreSQL ثابت` في رسالة الحالة. لا ينبغي الاعتماد على SQLite في Railway من دون Volume دائم.

## تدقيق التفاعل والأداء

كانت عمليات الشبكة المتزامنة موجودة داخل حلقة `asyncio` الخاصة بـ Telegram. شملت هذه العمليات جلب السعر، قراءة الرصيد، وتنفيذ البيع. مهلة HTTP تصل إلى 15 ثانية، ومع عدة صفقات كان يمكن أن يتوقف استقبال التفاعلات لعدة عشرات من الثواني.

تم نقل عمليات MEXC الرئيسية في مدير المراكز وأزرار الرصيد والبيع إلى `asyncio.to_thread`. بذلك تبقى حلقة Telegram قادرة على استقبال الأحداث أثناء انتظار الشبكة.

ما زالت عملية التحليل الطويلة تستخدم executor، وهذا صحيح. كما أن مدير المراكز يعالج الصفقات بالتسلسل. إذا زاد عدد الصفقات كثيراً، يمكن لاحقاً إضافة حد تزامن وcache للأسعار، لكن ذلك ليس مانعاً حالياً.

## أخطاء منطق التداول التي تم إصلاحها

كان `position_manager._update_sl` يفترض أن `trades_db.db()` يعيد cursor واحداً، بينما الدالة تعيد زوجاً من النوع والـ cursor. هذا كان يسبب استثناء بعد تحديث وقف الخسارة، وقد يقود إلى مسار الخروج الطارئ. تم تصحيح التعامل مع SQLite وPostgreSQL.

كان مسار الخروج الطارئ يغلق الصفقة في قاعدة البيانات حتى عندما يفشل البيع. تم تعديل السلوك بحيث تبقى الصفقة مفتوحة عند فشل البيع، مع تسجيل الخطأ، لأن إغلاق السجل في هذه الحالة يعطي حالة كاذبة.

كما تم منع مساري `close_winners` و`sell_all` من إغلاق الصفقة إذا أعادت المنصة نتيجة فشل أو إلغاء أو رفض.

## الاختبارات المنفذة

| الاختبار | النتيجة |
|---|---|
| `python3 -m compileall -q .` | ناجح |
| `git diff --check` | ناجح |
| اختبارات `unittest` الخمسة الموجودة | ناجحة بعد إصلاح حد التحويلات |
| دورة حفظ SQLite للإعدادات والصفقات | ناجحة |
| ترحيل schema قديمة مع الحفاظ على السجل | ناجح |

كانت اختبارات التحويلات تفشل قبل الإصلاح لأن الكود يستخدم حد `3` بينما الاختبارات تتوقع حد `10`. بعد توحيد الإعداد، نجحت الاختبارات الخمسة الموجودة.

## توصية النشر

ينبغي إضافة PostgreSQL إلى خدمة Railway وربط `DATABASE_URL` بالخدمة، ثم إعادة تشغيل البوت ومراجعة رسالة الحالة. إذا ظهرت عبارة `SQLite مؤقت` فلا تعتبر قاعدة البيانات جاهزة لتداول حقيقي.

كما ينبغي أخذ نسخة احتياطية من `trades.db` قبل أي تغيير في بيئة الإنتاج. وجود ملف قاعدة بيانات متعقب داخل Git غير مناسب كآلية نسخ احتياطي حية، لأن Git لا يحفظ التغييرات التشغيلية الجديدة تلقائياً.

## الملفات المعدلة

تم تعديل `config.py` و`trades_db.py` و`position_manager.py` و`bot.py`. وتم إنشاء هذا التقرير لتوثيق نتائج المراجعة.

## References

[1]: https://docs.python.org/3/library/asyncio-task.html#asyncio.to_thread "Python asyncio.to_thread documentation"
[2]: https://www.sqlite.org/wal.html "SQLite Write-Ahead Logging documentation"
[3]: https://www.postgresql.org/docs/current/tutorial-transactions.html "PostgreSQL transactions documentation"
